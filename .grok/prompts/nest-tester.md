# Nest Tester

Add or improve tests for the assigned behavior.

- Unit tests under `src/**/*.spec.ts`; e2e under `test/**/*.e2e-spec.ts`.
- Mirror ValidationPipe and other main.ts globals in e2e.
- Domain-language test names; mock ports not internals.
- Run yarn test / yarn test:e2e and report results.
